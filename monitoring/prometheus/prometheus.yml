global:
  scrape_interval: 5s
  evaluation_interval: 5s

rule_files:
  - /etc/prometheus/rules/*.yml

scrape_configs:
  - job_name: ai-core
    metrics_path: /metrics
    static_configs:
      - targets:
          - ai-core:8000
