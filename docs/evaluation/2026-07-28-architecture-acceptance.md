# 2026-07-28 电商 AI 八项目完整验收记录

## 验收结论

- 模式：`full`
- 运行编号：`75b74266-f649-470b-a674-cb47c0186386`
- 总用例：18
- 通过：16
- 警告：1
- 失败：0
- 主动跳过：1
- 已执行用例通过率：94.12%
- 总耗时：15744 ms
- Docker 容器状态：`healthy`

整体结论为：**通过，但存在明确记录的待完善项**。

## 八项目真实证据

| 项目 | 本次执行证据 | 结论 |
|---|---|---|
| 项目1 Agent Runtime | `/health`、`/ready`、`X-Request-ID`、Run指标 | 核心能力通过 |
| 项目2 售前咨询 Agent | 真实模型完成“油皮推荐→它有库存吗”两轮对话并复用会话 | 通过 |
| 项目3 轻量 AI 数据中台 | 5类数据资产、8项质量规则、激活版本和缓存指标 | 通过 |
| 项目4 直播切片 Agent | 真实抽取音轨、LLM规划、FFmpeg裁剪并登记1个MP4 | 通过 |
| 项目5 内容运营 Agent | 真实异步生成任务成功，草稿进入人工审核门禁 | 通过 |
| 项目6 MCP 共享层 | 标准客户端完成工具发现、Resource读取和库存工具调用 | 通过 |
| 项目7 多模态素材中心 | image/video/text可检索，保留来源和商品关联 | 通过 |
| 项目8 评测与可观测 | 最新v3固定评测30/30通过；HTTP告警健康 | 通过 |

## 本次主动执行的关键链路

### 售前咨询 Agent

- 首轮输入：推荐一款适合油皮的商品。
- 第二轮输入：那它现在有库存吗？
- 结果：系统完成商品搜索并在同一 `session_id` 下继承商品上下文，返回库存事实。

### 直播切片 Agent

- 使用 FFmpeg 生成并解析带音轨的测试视频；
- 从视频提取16kHz单声道音频；
- 将时间戳转写交给真实切片规划模型；
- 使用 FFmpeg 生成真实 MP4；
- 验证任务最终阶段为 `completed`。

### 内容运营 Agent

- 通过HTTP 202提交异步内容生成任务；
- 轮询到 `succeeded`；
- 查询到持久化草稿；
- 验证 `human_review_required=true`，未绕过人工审核。

### MCP

- 建立 Streamable HTTP MCP 会话；
- 发现商品、库存、订单和素材工具；
- 读取 `commerce://data-catalog`；
- 调用 `check_inventory(P1002)`。

## 警告与跳过说明

### 警告：真实批量 ASR 未配置

当前 `.env` 没有配置批量 ASR 服务地址和密钥，因此 `/ready` 中 `batch_asr=false`。本次直播切片使用确定性测试 Provider 验证 ASR 编排、时间戳落库和失败重试，不能将该结果表述为真实商业 ASR 识别准确率验收。

### 跳过：没有重复运行30条主动评测

当前最新 v3 评测记录已经达到30/30通过、工具选择准确率100%。本次架构验收读取了该持久化基线，没有再次产生30条模型调用。需要发布前重新执行时，可增加：

```powershell
--run-evaluation
```

## 边界

本结果来自个人电脑、模拟电商数据和本地 Docker 环境，只证明本次固定用例下的功能闭环。它不代表生产并发容量、真实用户规模、商业收益或第三方服务 SLA。
